# ArchUnit configuration for the field-service-api module.
# Frozen-violation store path: used by FreezingArchRule to persist known pre-existing violations.
# Violation store files are committed under VCS so new violations break the build.
default.violation.store.default.path=src/test/resources/archunit/frozen-violations
